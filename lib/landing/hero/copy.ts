/**
 * Hero statement copy. Kept in one place so layout tests can assert the
 * two-line composition without mounting WebGL or GSAP.
 *
 * The previous 15ch measure wrapped "It does not mark the answer" into a
 * nine-line stack that collided with the landing nav. These two lines are
 * sized to sit in the bottom band: four words, then four words.
 */
export const HERO_KICKER =
  "Final-year project. Adaptive misconception diagnosis.";

export const HERO_HEADLINE_LEAD = "Beyond Right or Wrong:";

export const HERO_HEADLINE_ACCENT = "We Decode Every Misconception";

export const HERO_HEADLINE = `${HERO_HEADLINE_LEAD} ${HERO_HEADLINE_ACCENT}`;
